const initialState = {
  addingRestroomInfo: {
    name: '',
    description: ''
  }
};

export default (state = initialState, action) => {
  /*eslint-disable indent */
  switch (action.type) {
    default:
      return state;
  }
};
